{
  "name": "invert-tailwind-colors",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "preview":"pnpm --filter=demo run dev",
    "build":"pnpm --filter=tailwind-color-invert run generate && pnpm --filter=demo run build",
    "generate":"pnpm --filter=tailwind-color-invert run generate"
  }
}